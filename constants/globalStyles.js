import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  text: {
    fontWeight: '500', // Áp dụng fontWeight cho tất cả văn bản
    fontSize: 15, // Định nghĩa cả fontSize
  },
});

export default globalStyles;
