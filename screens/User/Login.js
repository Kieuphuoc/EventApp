import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
const Login = () =>{
    return (
        <SafeAreaView>
            <Text>Chào tất cả các bạn</Text>
        </SafeAreaView>
    )
}

export default Login;